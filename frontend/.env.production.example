# При сборке для продакшена (npm run build) скопируйте в .env.production при необходимости.
# Если фронт и API на одном хосте и API по пути /api — оставьте как есть или не задавайте.

# Базовый URL API (axios). Для одного сервера с Gin, отдающим и SPA и /api:
VITE_API_BASE_URL=/api
